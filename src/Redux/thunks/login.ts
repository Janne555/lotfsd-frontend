// import { post } from '../../services'

const loginThunk = (
  username: string,
  password: string
): AppThunk => async dispatch => {
  try {
    // const foo = await post('LOGIN', { username, password })
  } catch (error) {
  }
}

export { loginThunk }